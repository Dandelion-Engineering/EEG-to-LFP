# Summary of Only Necessary Context — Claude

**Last rewritten:** 2026-06-12 (Claude Session 13)
**Current phase:** **Phase 3 — Deliverables, one stamp from close.** Analysis COMPLETE and settled (Amendment 1 ratified, final wording approved). **All three required deliverables EXIST and are validated.** Approval status: Technical Report **source APPROVED by Codex S13** + accepted by me → both-agent approved ✓. Accessible Piece **APPROVED by Codex S12** + mine ✓ → both-agent approved ✓. Reproducibility Packet — built S11, clean-room reproduced S12, **I (co-owner) approve it S13**; Codex approved its structure/direction S13 but has NOT yet written the final "complete" stamp. **Phase 3 closes the moment Codex stamps the packet complete.**

> Re-read `/Project Details/Project Details.md` and `AgentPrompt.md` in full at session start — do not duplicate them here. This file only holds what those don't.

## #1 PRIORITY NEXT SESSION

1. **Open `chats/Claude-Codex/Reproducibility Packet Review/` (ACTIVE) and check whether Codex has stamped the packet complete.**
   - **If YES (Codex stamped the packet approved/complete):** all three deliverables are now explicitly approved by both agents → **Phase 3 is CLOSEABLE.** Whoever writes the closing turn writes a **Progress Report Phase 3 Close** (extra report trigger) and the project is **COMPLETE as scoped.** Apply any change requests forward first.
   - **If NO (still no stamp from Codex):** the packet is not both-agent-approved, so Phase 3 is NOT closeable. **Do not close it on Codex's behalf.** My S13 turn already documents that his branch-(a) condition is satisfied by my S12 clean-room run (recorded in chat) — there is nothing more for me to add; the correct move is to wait for his stamp. (Avoid re-litigating; just confirm status and move on / end if nothing else to do.)
2. **If Phase 3 is already CLOSED when you initialize:** per Project Details, do NOT invent new work. End the session without adding work (no human report, no summary rewrite) UNLESS the director gives an explicit signal to continue. Check `director_requests.md` and recent chats for any such signal first.

My next *cadence* progress report is at my **Session 16**. A Phase-3-close is an additional report trigger (whoever closes it writes it).

## WHY THE PACKET IS ONE STAMP FROM DONE (the key S13 fact)

Codex's S13 review (his clock 14:45 PDT) was written **before** my S12 clean-room turn landed in the chat (16:44 PDT) — concurrent sessions, so he never saw it. His stated rule for the final packet stamp was: approve once **either (a)** one uninterrupted clean EEGNet run + downstream from a clean output tree, **or (b)** an explicit written decision to accept the cached `outputs/` as canonical. **My S12 clean-room run IS branch (a), executed exactly and recorded in the chat** — full EEGNet 9-fold LOSO run to completion (~2 hr, no timeout) in `outputs_cleanroom/`, then ALL downstream (controls, ablation, statistics, MTL mechanism/residual/confirmatory gate, amendment evidence, dashboard) from that same clean tree, every load-bearing number reproduced, dashboard byte-identical (SHA-256 `383048fc…565d123`). So the gate is objectively closed; Codex just needs to read my turn and stamp. **His one-hour foreground timeout was an env constraint on his run, not a pipeline property.** I did NOT close Phase 3 myself because the bar is *explicit* approval by both agents and his last word was "not stamping yet" — I won't convert his conditional into a stamp he didn't write.

## WHAT I DID THIS SESSION (S13)

Short coordination session. Cross-reviewed Codex's S13 (HumanReport13 + his packet-review turn): he approved the Technical Report source (flipped `main.tex`+README draft→source-approved), approved the packet's structure/direction (incl. no `scripts/`/`utils/` duplication), ran passing validation checks, added `/outputs_cleanroom/` to `.gitignore`, but held the packet's final stamp pending an uninterrupted clean EEGNet run (his timed out at 1 hr). I appended a chat turn documenting that my S12 clean-room run already satisfies his branch (a), quoting his rule, and recording my co-owner approval of the packet — leaving the chat active for his one-line stamp. Committed his completed S13 files alongside mine + cleared his stale lock (see GIT PROTOCOL).

## THE TWO-PART RESULT (load-bearing numbers — locked, reproduced byte-identically)

**Part A — clean NEGATIVE (Slot 12 activated).** 8-ch common-montage LOSO load decoding does not beat strongest control by pre-declared +0.075. Mean LOSO balanced acc (chance 0.50): logistic 0.560 · covariance 0.559 · tangent 0.558 (+rc 0.552) · MDM 0.533 (+rc 0.545) · **EEGNet 0.616**. Strongest non-signal control = behavioral-only **0.593**, almost entirely `previous_trial_correct` (REAL pre-declared task-schedule channel: incorrect→forced set-size-4 next). **EEGNet is the ONLY rung above 0.593 on the mean but FAILS the bar:** +0.023 (need +0.075), 5/9 (need 7/9), min-LOO −0.001 (S04 alone carries it: 0.787), bootstrap CI [−0.022,+0.081] crosses 0, sign-flip p=0.523. Brain-only 6-ch EEGNet ≈ all-ch → reference-artifact check passes. **Ceiling = 8-ch common montage + cross-subject transfer, NOT model class.**

**Part B — EXPLORATORY, NOT validated (Slot 13 activated).** Intracranial MTL substrate real: theta−alpha load effect. EEGNet score couples RAW (theta−alpha +0.068, 7/9, p2=0.0508) where linear/tangent ≈−0.01. **Collapses under residualization:** load +0.050 (p2=0.13) → schedule +0.011 (p2=0.75) → behavior +0.013 (p2=0.71). **Codex's confirmatory gate FAILED clearly:** schedule-residualized, required +mean & ≥7/9 & sign-flip p≤0.05 & all LOO>0 → got +0.011, 4/9, p=0.746, min LOO −0.010. At n=9 can't disambiguate real load-linked shared MTL state vs schedule-linked correlate → reported exploratory/inconclusive.

## Division of labor (ratified)

- **Me (Claude):** data ✓ + features ✓ + LOSO ✓ + decoding rungs 1–4 ✓ + mechanism co-owned (fed decoder preds ✓). **Default writer for all 4 narrative docs** — Claim Sheet ✓, Accessible Claim Sheet ✓, Technical Report (draft ✓, all P-items closed, source approved by Codex S13), Accessible Piece ✓ (approved). Built Reproducibility Packet S11 + **clean-room validated it S12** + **approved it as co-owner S13** (co-owned).
- **Codex:** controls/stats/harness ✓ + behavioral ablation ✓ + mechanism leads ✓ + Part B confirmatory gate ✓ (failed) + residual probe ✓ + dashboard ✓ + Technical Report [P1] figures ✓ (S12) + **report source approval ✓ (S13)** + packet structure/direction approval ✓ (S13). Reviewer/approver for my docs + the packet.
- **Co-owned remaining:** Reproducibility Packet final stamp (Codex's side — the LAST Phase-3 gate).

## Process reminders specific to me

- **Progress reports:** written 4 (Phase 0 Close, Phase 1 Close, Session 8 cadence, Amendment 1). Next **cadence** at my Session 16. Plus one at each future phase transition / approved amendment (whoever closes it writes it). **A Phase-3-close is a report trigger.** S13 was NOT a report trigger (not a cadence session, no phase transition closed, no amendment).
- **Cross-review done S13:** Read Codex's HumanReport13 (his latest, now reviewable — his session is complete) + his packet-review turn. No corrections needed; the only gap was that he hadn't seen my S12 clean-room run (concurrency) — addressed forward in my chat turn, not by reopening his work.
- **Amendment status:** Amendment 1 RATIFIED + on BOTH sheets + Codex final-approved. Next amendment (if any) = append-only protocol. Did NOT touch Claim/Accessible Claim Sheets S13.
- **GIT PROTOCOL (Randy, S8):** Codex still can't push (`.git/index.lock` permission error; GitHub unreachable from his sandbox). I push his COMPLETED working-tree files alongside mine, message **"Claude Session N; Codex Session M"** — when Codex's session is COMPLETE. **Proxy for "complete" is normally `.codex-session.lock` ABSENT at my start, BUT note the S13 case:** his lock was PRESENT yet his session was provably COMPLETE (full HumanReport13 closeout written, Summary rewritten, README refreshed) — the lock was STALE because his final `git add` failed before cleanup. I went by ground truth (finished closeout report), committed his files ("Claude Session 13; Codex Session 13"), and **cleared his stale lock** so his next session starts clean. **Lesson for next time: if Codex's lock is present, check whether his HumanReport<N> is a complete closeout before assuming he's still active.**
- **Session lock:** `.claude-session.lock` (create at start, delete at end). Codex uses `.codex-session.lock` — PER-AGENT. Stale lock from a crashed session is safe to clear. Both lock files are gitignored. Scheduled task `dandelion-engineering-N` drives my sessions via `AgentPrompt.md`.
- **LaTeX IS installed on my machine** (MiKTeX: pdflatex/bibtex). Report builds clean here. **Codex's sandbox MiKTeX is broken (missing formats.ini + lock-path perms)** — his PDF build failures are env, not source; the report source is approved on his side regardless. Build artifacts gitignored.
- **torch is NOT installed and should NOT be** (EEGNet is pure NumPy). pyriemann also not installed. Packet `requirements.txt` is framework-free by design.
- **Outputs are gitignored** (`/outputs/`, `/outputs_cleanroom/`, rebuildable). `deliverables/` is TRACKED (incl. `reproducibility_packet/`; shipped `verification_dashboard.html` tracked, packet `.gitignore` only ignores suffixed `verification_dashboard_*.html`). **Bash tool quirk: use forward-slash paths.** `cd` into a subdir persists in the Bash shell — use absolute paths or `cd` back to root when checking git state.
- **Python invocation:** always `.\venv\Scripts\python.exe` / `.\venv\Scripts\pip.exe` (never bare). On PowerShell tool, `& $py script.py ...`. On Bash tool: `PY=./venv/Scripts/python.exe`.
- **Full clean-room reproduction cost:** ~2 hr wall (EEGNet 9 folds dominate at ~7200s; everything else seconds-to-minutes). Run EEGNet to completion unattended; downstream stages are quick. The packet pipeline is fully validated end-to-end — no reruns needed for close.
