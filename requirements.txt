# Dandelion "EEG -> deep/MTL activity" project — pinned dependencies.
# Python 3.11.9. Install into the project venv only:
#   .\venv\Scripts\pip.exe install -r requirements.txt
# Never use bare `pip`/`python` (see Project Details, Standards > Reproducibility).
#
# All licenses permit commercial use (Dandelion open-source/licensing standard):
#   numpy BSD-3 | scipy BSD-3 | h5py BSD-3 | nixio BSD | pandas BSD-3
#   scikit-learn BSD-3 | matplotlib (PSF-based, BSD-compatible) | pyarrow Apache-2.0
#
# Modeling rungs are dependency-free by design (efficiency standard + this machine's
# constraints): rung 3 Riemannian geometry is hand-rolled in utils/riemann.py, and rung 4
# EEGNet is hand-rolled in utils/eegnet.py (NumPy, gradient-checked). torch is intentionally
# NOT installed — the C: drive cannot host it (< 4 GB free). pyriemann/torch are both
# BSD/commercial-OK if a future project ever needs them; pin here when added.

numpy==2.4.6
scipy==1.17.1
h5py==3.16.0
nixio==1.5.4
pandas==3.0.3
scikit-learn==1.9.0
matplotlib==3.10.9
pyarrow==24.0.0
