# Reproducibility Packet — pinned dependencies.
# Reproduced result was produced on Python 3.11.9.
#
# Install into a project virtual environment from the REPOSITORY ROOT, using the
# venv interpreter explicitly (never a bare `pip`):
#   Windows:        .\venv\Scripts\pip.exe install -r deliverables\reproducibility_packet\requirements.txt
#   Linux / macOS:  ./venv/bin/pip install -r deliverables/reproducibility_packet/requirements.txt
#
# All licenses permit commercial use:
#   numpy BSD-3 | scipy BSD-3 | h5py BSD-3 | nixio BSD | pandas BSD-3
#   scikit-learn BSD-3 | matplotlib (PSF, BSD-compatible) | pyarrow Apache-2.0
#
# The two heaviest modeling rungs are framework-free by design: the Riemannian
# classifier (utils/riemann.py) and EEGNet (utils/eegnet.py) are hand-implemented
# in NumPy and gradient-checked. No PyTorch / TensorFlow dependency — the full
# pipeline runs on a consumer-laptop CPU.

numpy==2.4.6
scipy==1.17.1
h5py==3.16.0
nixio==1.5.4
pandas==3.0.3
scikit-learn==1.9.0
matplotlib==3.10.9
pyarrow==24.0.0
